<Application x:Class="SonyBraviaControl.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#0B0D10" />
        <SolidColorBrush x:Key="PanelBackground" Color="#14181D" />
        <SolidColorBrush x:Key="ButtonBackground" Color="#20262D" />
        <SolidColorBrush x:Key="ButtonHover" Color="#2C343D" />
        <SolidColorBrush x:Key="AccentBrush" Color="#66D3C8" />
        <SolidColorBrush x:Key="DangerBrush" Color="#F06A6A" />
        <SolidColorBrush x:Key="TextPrimary" Color="#F3F5F7" />
        <SolidColorBrush x:Key="TextSecondary" Color="#9CA7B2" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource AppBackground}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
        </Style>

        <!-- TextBlock does not reliably inherit Window.Foreground through every WPF
             template/container. Set it explicitly so dark theme text can never turn black. -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
        </Style>

        <Style x:Key="RemoteButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="#2F3842" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ButtonHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.72" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource RemoteButton}">
            <Setter Property="Background" Value="#173A37" />
            <Setter Property="BorderBrush" Value="#2C7770" />
            <Setter Property="Foreground" Value="#B9FFF7" />
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource RemoteButton}">
            <Setter Property="Background" Value="#401D22" />
            <Setter Property="BorderBrush" Value="#7D343E" />
            <Setter Property="Foreground" Value="#FFD6DB" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0F1317" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="BorderBrush" Value="#34404A" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
        </Style>
    </Application.Resources>
</Application>
